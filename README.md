# What-s-Around-Me
A location-based discovery app that helps users find interesting places near their current location. The app display's nearby points of interest on an interactive map and in a searchable list.

To start the iOS app on iPhone install Expo Go application and run `npx expo start`.